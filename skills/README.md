# Skills

把 Codex 风格的 Skill（`SKILL.md` / `SKILL.cn.md`，可选 `meta.yaml` 和 `references/`）放到这里，
`MiniMax H3 Multimodal Chat` 节点即可在 `skill` 下拉框中加载。

本插件同时会自动发现 `custom_nodes/comfyUI-llama-TE/skills` 下的 Skill。
